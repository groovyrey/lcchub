name: portal_app
description: "LCC Hub Student Portal"
publish_to: 'none'
version: 1.0.1+2

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  shared_preferences: ^2.2.0
  provider: ^6.1.0
  google_fonts: ^6.2.0
  flutter_markdown: ^0.7.0
  flutter_local_notifications: ^18.0.0
  timezone: ^0.10.0
  intl: ^0.20.0
  permission_handler: ^12.0.3
  package_info_plus: ^9.0.1
  url_launcher: ^6.3.2
  phosphor_flutter: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
